import { Router } from 'express'; import Responder from '../models/Responder.js'; import { requireAuth,allowRoles } from '../middleware/auth.js'; const router=Router(); router.use(requireAuth,allowRoles('dispatcher','admin')); router.get('/',async(req,res)=>res.json(await Responder.find().sort('type label'))); router.patch('/:id',async(req,res)=>{const unit=await Responder.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true});if(!unit)return res.status(404).json({error:'Responder not found'});res.json(unit);}); export default router;
